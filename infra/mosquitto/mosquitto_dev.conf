# IoTAPS Mosquitto DEV config — allows anonymous connections for local testing
persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883
protocol mqtt

listener 9001
protocol websockets

# DEV ONLY: allow anonymous for local testing without auth backend
allow_anonymous true
