[pytest]
# Enable pytest-asyncio in auto mode so `async def` tests (and those marked
# with @pytest.mark.asyncio) run without per-test event-loop boilerplate.
asyncio_mode = auto
testpaths = app
python_files = test_*.py
